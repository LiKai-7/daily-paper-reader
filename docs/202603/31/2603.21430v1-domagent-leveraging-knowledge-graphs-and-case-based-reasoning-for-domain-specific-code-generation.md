---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱和基于案例的推理进行特定领域代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v1"
tags: ["query:mmkgc-llm"]
score: 9.0
evidence: 利用知识图谱和检索的自主编程代理
tldr: 针对通用大模型在特定领域代码生成中因缺乏专业知识而表现不佳的问题，本文提出 DomAgent 框架。该框架通过核心组件 DomRetriever，巧妙结合了知识图谱的自顶向下推理与案例推理的自底向上检索，模拟人类获取领域知识的过程。实验表明，DomAgent 显著提升了代码生成的准确性，使小型开源模型在复杂领域任务中能达到甚至接近闭源大模型的性能水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21430v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison (pass@1) across different libraries and methods on Data Science dataset. All the results marked with an asterisk (*) are copied from the Magicoder paper [43].\", \"page\": 7, \"index\": 1, \"width\": 1048, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2603.21430v1/fig-002.webp\", \"caption\": \"Figure 3: Effect of KG-based case selection versus random sampling with different sampling proportions on case base construction, using the Qwen2.5-7B-based DomAgent on the Data Science dataset.\", \"page\": 7, \"index\": 2, \"width\": 486, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.21430v1/fig-003.webp\", \"caption\": \"Figure 2: Case base construction (top) and the overall workflow of our DomAgent (bottom). We first perform KG–guided selection to obtain representative cases. Then, our DomAgent retrieves domain knowledge from a KG and cases from the case base, while reviewing the retrieved domain knowledge in the reasoning process based on the selected case. Finally, using the answer of DomAgent, LLM generates the domain-specific code.\", \"page\": 4, \"index\": 3, \"width\": 1046, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2603.21430v1/fig-004.webp\", \"caption\": \"Table 4: Ablation study on the truck CAN signal code generation task.\", \"page\": 8, \"index\": 4, \"width\": 1044, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.21430v1/fig-005.webp\", \"caption\": \"Table 3: Ablation study of different modules on the Data Science dataset.\", \"page\": 8, \"index\": 5, \"width\": 1048, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2603.21430v1/fig-006.webp\", \"caption\": \"Figure 1: Example of a domain-specific code generation task in truck software development: reading the CAN signal OverspeedWarningStat and adapting the function with context-specific mapping to fit the workflow.\", \"page\": 1, \"index\": 6, \"width\": 498, \"height\": 296}]"
motivation: 通用大模型在处理需要高度专业化知识的特定领域任务时，常因训练数据中缺乏相关领域知识而导致代码生成成功率低下。
method: 提出 DomAgent 框架，利用 DomRetriever 模块动态整合知识图谱的结构化推理与基于案例的经验检索，实现领域知识的精准适配。
result: 在 DS-1000 基准测试和真实工业软件开发任务中，DomAgent 显著增强了模型的生成效果，有效缩小了开源模型与顶级商业模型间的差距。
conclusion: 通过结合概念理解与经验学习的混合检索机制，DomAgent 为大模型在特定领域的自主代码生成提供了一种高效且灵活的适配方案。
---

## 摘要
大语言模型（LLMs）在代码生成方面展现出了卓越的能力。然而，由于大多数 LLM 是在公共领域语料库上训练的，直接将其应用于现实世界的软件开发往往成功率较低，因为这些场景通常需要特定领域的知识。特别是，特定领域的任务通常需要高度专业化的解决方案，而这些方案在通用 LLM 的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了 DomAgent，这是一种自主编程智能体，通过结构化推理和针对性检索，使 LLM 能够生成领域自适应代码，从而弥补这一差距。DomAgent 的核心组件是 DomRetriever，这是一种新颖的检索模块，它通过结合概念理解与经验案例，模拟人类学习特定领域知识的方式。它动态地整合了自顶向下的知识图谱推理和自底向上的基于案例的推理，实现了结构化知识和代表性案例的迭代检索与综合，从而确保了上下文相关性和广泛的任务覆盖范围。DomRetriever 既可以作为 DomAgent 的一部分运行，也可以独立与任何 LLM 配合使用，以实现灵活的领域自适应。我们在数据科学领域的开放基准数据集（DS-1000）上评估了 DomAgent，并将其进一步应用于现实世界的卡车软件开发任务。实验结果表明，DomAgent 显著增强了特定领域的代码生成能力，使小型开源模型在复杂的现实应用中能够缩小与大型闭源 LLM 之间的大部分性能差距。代码可在以下网址获取：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

### 论文总结：DomAgent —— 结合知识图谱与案例推理的特定领域代码生成智能体

#### 1. 核心问题与整体含义（研究动机和背景）
通用大语言模型（LLMs）在处理特定领域（Domain-Specific）的代码生成任务时面临两大挑战：
*   **领域知识匮乏**：LLM 训练数据多源于公共代码库，缺乏对企业内部第三方库、私有 API 或特定行业协议（如卡车 CAN 总线信号）的深入理解。
*   **上下文适配能力不足**：特定领域的开发往往涉及独特的工作流逻辑和约束，通用模型难以直接生成符合特定业务规范的代码。
*   **现有方案局限**：微调（Fine-tuning）成本高且难以实时更新；传统 RAG（检索增强生成）在处理复杂逻辑时，检索到的长文本可能干扰模型注意力，且缺乏对知识结构的系统性把握。

#### 2. 方法论：核心思想与关键技术
DomAgent 的核心思想是**模拟人类学习过程**：结合“自顶向下”的结构化概念理解（知识图谱）与“自底向上”的经验实例学习（案例推理）。

*   **核心组件：DomRetriever**
    *   **自顶向下检索（Top-down）**：利用知识图谱（KG）存储包、函数及其层级关系。通过 LLM 分类器确定任务所属包，并基于语义相似度从 KG 中提取精确的 API 定义和参数描述。
    *   **自底向上检索（Bottom-up）**：从案例库中检索相似的代码实现。
    *   **动态推理与审查**：引入推理型 LLM（类似 DeepSeek-R1 的 CoT 模式），通过 `<think>` 标签自主调用 `SearchKG` 和 `SearchCase` 工具。模型会对比检索到的 KG 知识与案例代码，过滤掉语义相关但功能无关的冗余信息。
*   **KG 引导的层次化案例选择**
    *   为了用极小的案例库实现高覆盖率，DomAgent 利用 KG 的结构对函数进行聚类，优先选择能覆盖更多“功能簇”的代表性案例，而非随机采样。
*   **智能体训练**
    *   **SFT（监督微调）**：教模型学会使用特定的检索工具触发词。
    *   **RL（强化学习）**：采用 **GRPO 算法**，利用外部强大模型作为奖励模型（Reward Model），评估检索内容与任务的相关性，优化智能体的推理和过滤能力。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **DS-1000**：涵盖 NumPy、Pandas、PyTorch 等 7 个主流数据科学库的 1000 个任务。
    2.  **Truck CAN Signal**：真实工业场景，涉及卡车 CAN 信号的读写任务，包含 776 个信号，跨越 6 个功能域（如动力、能源、视觉等）。
*   **Benchmark 与对比方法**：
    *   **基础模型**：GPT-4o, GPT-3.5, LLaMA-3.1 (8B), Qwen-2.5 (7B), LLaMA-3.3 (70B)。
    *   **专业编程模型**：StarCoder, WizardCoder, Magicoder。
    *   **对比架构**：Vanilla（原始模型）、标准 RAG Pipeline、DomAgent 架构。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   微调阶段：500 个开源任务示例。
    *   强化学习阶段：2 个 Epoch，Batch Size 为 16，学习率为 1e-6。
    *   推理阶段：每个 Query 生成 16 个 Rollouts 进行采样优化。

#### 5. 实验数量与充分性
*   **实验规模**：在 DS-1000 上使用了 700 个测试用例，在卡车领域使用了 776 个真实信号任务。
*   **消融实验**：非常充分。分别验证了“仅 KG”、“仅 CBR（案例）”、“标准 RAG”以及“DomAgent（带推理审查）”的效果。
*   **公平性**：对比了同参数规模（7B/8B）的模型，并展示了 DomAgent 如何帮助小模型跨越代差挑战大模型（如 GPT-4o）。

#### 6. 主要结论与发现
*   **性能飞跃**：DomAgent 使 Qwen-2.5-7B 在卡车领域的 Pass@1 从 39.62% 提升至 **96.64%**，几乎追平了增强后的 GPT-4o。
*   **小模型逆袭**：搭载 DomRetriever 的 LLaMA-3.1-8B 在 DS-1000 上的表现优于经过大规模微调的 MagicoderS-CL。
*   **检索效率**：KG 引导的案例选择方法仅需 **30%** 的数据量即可达到随机采样 80% 数据量的性能，显著降低了工业界构建案例库的成本。
*   **协同效应**：DomRetriever 具有通用性，作为插件配合 GPT-4o 使用时，能将其代码生成准确率进一步提升约 7.6%。

#### 7. 优点（亮点）
*   **混合推理机制**：将结构化的 KG 逻辑与非结构化的代码案例结合，解决了 RAG 检索不精准的问题。
*   **工业落地性强**：直接在沃尔沃卡车工厂部署并验证，证明了其在处理高度专业化、私有化协议任务中的实用价值。
*   **自主审查能力**：通过强化学习让 Agent 具备了“反思”检索结果的能力，减少了长上下文带来的噪声干扰。

#### 8. 不足与局限
*   **语言限制**：目前主要针对 Python 语言进行了验证，尚未探讨在 C++ 或 Java 等其他工业常用语言上的表现。
*   **KG 依赖性**：系统性能高度依赖于领域知识图谱的质量，而高质量 KG 的构建和维护仍需要一定的专家经验。
*   **结构化深度**：目前主要利用了包和函数的层级关系，尚未深入利用抽象语法树（AST）进行更细粒度的代码结构检索（作者将其列为未来工作）。

（完）
