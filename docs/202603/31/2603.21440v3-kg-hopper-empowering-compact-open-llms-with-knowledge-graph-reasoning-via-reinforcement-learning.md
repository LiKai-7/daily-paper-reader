---
title: "KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning"
title_zh: KG-Hopper：通过强化学习赋予紧凑型开源大语言模型知识图谱推理能力
authors: "Shuai Wang, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21440v3"
tags: ["query:mmkgc-llm"]
score: 10.0
evidence: 通过强化学习增强大模型的知识图谱推理
tldr: 针对大语言模型在知识图谱多步推理中存在的错误累积和灵活性不足问题，本文提出 KG-Hopper 框架。该框架利用强化学习训练轻量级开源模型（如 7B），将其推理过程整合进单一推理周期的“思考”阶段，实现全局路径探索与回溯。实验证明，该方法在多个基准测试上超越了参数量更大的模型及部分闭源模型，展现了极高的效率与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21440v3/fig-001.webp\", \"caption\": \"TABLE III: Ablation study of the RL training process. The table reports performance across different RL variants compared to the baseline Qwen-2.5-7B (RL) + KG model. Reported (+/–) values indicate the change in Hits@1 relative to the baseline.\", \"page\": 7, \"index\": 1, \"width\": 980, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.21440v3/fig-002.webp\", \"caption\": \"Fig. 2: The RL training process under two settings: with and without history resampling (RQ4). The figure shows how reward, response length, and retrieval count change over training steps.\", \"page\": 7, \"index\": 2, \"width\": 961, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2603.21440v3/fig-003.webp\", \"caption\": \"TABLE I: Performance comparison (Hits@1). Results marked with ‘*’ are taken directly from the corresponding original papers. Bold numbers indicate the best performance, while underlined numbers denote the second-best.\", \"page\": 6, \"index\": 3, \"width\": 979, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2603.21440v3/fig-004.webp\", \"caption\": \"Fig. 1: Multi-step vs one-round multi-hop reasoning over a knowledge graph: (a) multi-step reasoning and (b) our one-round reasoning. The multi-step pipeline invokes multiple sequential LLM calls and fails due to the missing entity Yellow Hibiscus, leading to an incorrect path. In contrast, our one-round approach performs the entire reasoning process within a single Reasoning LLM call, maintaining coherence and demonstrating robustness to incomplete knowledge.\", \"page\": 2, \"index\": 4, \"width\": 976, \"height\": 633}]"
motivation: 现有的知识图谱问答方法多采用分步推理，容易导致错误累积且缺乏对全局路径的动态调整能力。
method: 提出一种基于强化学习的框架，将知识图谱的遍历与决策过程统一嵌入到模型的“思考”阶段，支持动态探索与回溯。
result: 基于 7B 参数的模型在 8 个基准测试中优于 70B 的多步推理系统，并达到了与 GPT-4o-mini 相当的水平。
conclusion: KG-Hopper 证明了通过强化学习增强轻量级开源模型的全局推理能力，是解决复杂知识密集型任务的高效方案。
---

## 摘要
大语言模型（LLMs）展现了出色的自然语言能力，但在处理知识密集型推理任务时往往面临挑战。利用结构化知识图谱（KGs）的知识库问答（KBQA）由于需要准确的多跳推理，充分体现了这一挑战。现有方法通常在预定义流水线的指导下执行顺序推理步骤，这限制了灵活性，并由于每一步的孤立推理而导致错误级联。为了解决这些局限性，我们提出了 KG-Hopper，这是一个新颖的强化学习（RL）框架，旨在赋予紧凑型开源 LLM 在单轮推理中执行集成多跳 KG 推理的能力。我们没有采用逐步推理的方式，而是训练了一个推理 LLM，将整个 KG 遍历和决策过程嵌入到一个统一的“思考”阶段，从而实现对跨步依赖关系的全局推理以及带有回溯的动态路径探索。在八个 KG 推理基准测试上的实验结果表明，基于 7B 参数 LLM 的 KG-Hopper 始终优于更大的多步系统（高达 70B），并达到了与 GPT-3.5-Turbo 和 GPT-4o-mini 等专有模型相当的性能，同时保持了紧凑、开源和数据高效的特点。代码已在以下地址公开：https://github.com/Wangshuaiia/KG-Hopper。

## Abstract
Large Language Models (LLMs) demonstrate impressive natural language capabilities but often struggle with knowledge-intensive reasoning tasks. Knowledge Base Question Answering (KBQA), which leverages structured Knowledge Graphs (KGs) exemplifies this challenge due to the need for accurate multi-hop reasoning. Existing approaches typically perform sequential reasoning steps guided by predefined pipelines, restricting flexibility and causing error cascades due to isolated reasoning at each step. To address these limitations, we propose KG-Hopper, a novel Reinforcement Learning (RL) framework that empowers compact open LLMs with the ability to perform integrated multi-hop KG reasoning within a single inference round. Rather than reasoning step-by-step, we train a Reasoning LLM that embeds the entire KG traversal and decision process into a unified ``thinking'' stage, enabling global reasoning over cross-step dependencies and dynamic path exploration with backtracking. Experimental results on eight KG reasoning benchmarks show that KG-Hopper, based on a 7B-parameter LLM, consistently outperforms larger multi-step systems (up to 70B) and achieves competitive performance with proprietary models such as GPT-3.5-Turbo and GPT-4o-mini, while remaining compact, open, and data-efficient. The code is publicly available at: https://github.com/Wangshuaiia/KG-Hopper.

---

## 论文详细总结（自动生成）

这是一份关于论文《KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：大语言模型（LLMs）在知识密集型任务中常出现幻觉。虽然知识图谱问答（KBQA）能提供结构化事实，但现有的多跳推理方法多采用“分步调用”的流水线模式。
*   **核心痛点**：
    1.  **灵活性差**：预定义的推理路径难以应对知识图谱缺失或误导性数据。
    2.  **错误级联**：早期步骤的错误会直接传播到后续，且缺乏回溯机制。
    3.  **局部最优**：分步推理忽略了步骤间的全局依赖关系。
*   **整体含义**：本文旨在通过强化学习（RL）训练，让紧凑型开源模型（如 7B）在单次推理的“思考（Thinking）”阶段内自主完成复杂的知识图谱遍历、决策与回溯。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将多跳推理过程转化为模型内部的“思维链（CoT）”，通过特殊标签（如 `<search>`）实现模型与外部工具的交互，并利用 RL 优化整个推理轨迹。
*   **关键技术流程**：
    1.  **冷启动（Cold Start）**：构建 500 条高质量 CoT 数据进行监督微调（SFT），教会模型基本的工具调用格式和结构化输出。
    2.  **强化学习优化（GRPO）**：采用组相对策略优化（Group Relative Policy Optimization），通过采样多个推理路径并计算相对优势来稳定训练。
    3.  **复合奖励函数设计**：
        *   **检索奖励 ($R_{search}$)**：鼓励使用工具，但设上限防止过度调用。
        *   **格式奖励 ($R_{format}$)**：确保模型遵循 `<think>`、`<search>`、`<answer>` 的结构。
        *   **推理奖励 ($R_{reason}$)**：利用大模型（如 Llama-70B）作为评估者，对推理逻辑的合理性打分。
        *   **答案奖励 ($R_{answer}$)**：判断最终答案与真实值的语义一致性。
    4.  **训练技巧**：
        *   **掩码处理**：在计算损失时掩盖检索到的三元组内容，防止模型机械模仿外部知识。
        *   **历史重采样**：在训练后期移除简单的一跳问题，强迫模型学习复杂的多跳推理。

### 3. 实验设计
*   **数据集**：涵盖 8 个主流基准测试，基于 Freebase（CWQ, WebQSP, WebQuestions, GrailQA）和 WikiData（QALD10-en, T-REx, Zero-Shot RE, Creak）。
*   **对比方法**：
    *   **纯 Prompt 方法**：Qwen-2.5-7B, Llama-3.1-8B, Llama-3.3-70B, GPT-4o-mini, GPT-4o。
    *   **知识增强（无微调）**：上述模型结合 KG 检索工具。
    *   **SFT 微调方法**：Interactive-KBQA, KG-CoT 等。
*   **评估指标**：主要使用 Hit@1，Creak 数据集使用准确率（Accuracy）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练参数**：训练 2 个 Epoch，Batch Size 为 16，学习率为 1e-6。
*   **推理设置**：每个查询生成 16 个 Rollouts（采样路径）。
*   **训练时长**：文中未明确给出总训练小时数，但提到仅使用了 2000 条样本进行 RL 训练，数据效率较高。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同性质的数据集上进行了测试，涵盖了问答、槽填充和事实核查任务。
*   **消融实验**：针对 RL 奖励组成（检索、格式、推理、答案）、有无真实标签（Judge vs Evaluator）以及重采样策略进行了详细对比。
*   **充分性评价**：实验设计较为全面，对比了从 7B 到 70B 的多种模型规模，并与闭源最强模型（GPT-4o）进行了对标，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 RL 训练的 7B 模型在多项指标上超越了 70B 的多步推理系统。
*   **单轮推理优势**：将推理嵌入“思考”阶段能有效处理知识缺失，支持动态回溯，表现优于传统的分步 Pipeline。
*   **RL 的必要性**：RL 相比 SFT 能显著提升模型在复杂长链推理中的鲁棒性（提升 4%-10%），且对标注数据的依赖更低。
*   **奖励机制作用**：推理过程奖励（Reasoning Reward）对性能贡献最大，证明了“逻辑质量”比“最终答案”在训练中更重要。

### 7. 优点与亮点
*   **高效性**：通过单次 LLM 调用完成多跳推理，减少了多次 API 调用的延迟和成本。
*   **自主性**：模型展现了类似 OpenAI o1 的“自我反思”能力，能在发现检索信息不足时自动调整搜索策略。
*   **数据效率**：仅需极少量的 RL 训练数据（2000 条）即可实现显著的性能跨越。
*   **开源友好**：基于开源模型构建，且代码公开，具有很强的实用价值。

### 8. 不足与局限
*   **外部模型依赖**：训练过程中需要更强大的模型（如 Llama-70B 或 GPT-4o）作为奖励评估者，这增加了训练初期的成本。
*   **知识图谱规模限制**：实验主要基于 Freebase 和 WikiData，对于极度稀疏或私有领域的知识图谱，其泛化能力尚待验证。
*   **推理开销**：虽然是单轮调用，但由于生成了长大的“思考”内容（Thinking tokens），推理时的 Token 消耗和时间会显著增加。

（完）
