---
title: "GAAMA: Graph Augmented Associative Memory for Agents"
title_zh: GAAMA：面向智能体的图增强关联记忆
authors: "Swarna Kamal Paul, Shubhendu Sharma, Nitin Sareen"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27910v1"
tags: ["query:mmkgc-llm"]
score: 9.0
evidence: 为智能体构建基于层次化知识图谱的图增强关联记忆
tldr: GAAMA 是一种为 AI 智能体设计的图增强关联记忆系统，旨在解决多轮对话中长期记忆的结构化存储与检索问题。它通过 LLM 提取原子事实、主题概念和高阶反思，构建包含四类节点和五类边的层次化知识图谱。检索时结合了语义相似度与个性化 PageRank 算法，在 LoCoMo-10 基准测试中表现优于 RAG 和 HippoRAG 等基准，显著提升了记忆检索的准确性和关联性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27910v1/fig-001.webp\", \"caption\": \"Table 7: Heatmap of reward delta (pp): PPR=0.1 − Semantic, per conversation and category.\", \"page\": 10, \"index\": 1, \"width\": 629, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-002.webp\", \"caption\": \"Table 1: GAAMA graph schema: four node types connected by five structural edge types.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-003.webp\", \"caption\": \"Table 6: Per-conversation overall reward (%) for Semantic vs. PPR=0.1. ∆ = PPR=0.1 − Semantic (pp).\", \"page\": 9, \"index\": 3, \"width\": 527, \"height\": 276}]"
motivation: 现有的 RAG 和向量检索方法难以捕捉多轮对话中的关联结构和层次化逻辑，且现有图技术存在中心节点主导检索的问题。
method: 构建一个包含片段、事实、反思和概念四类节点的层次化知识图谱，并结合余弦相似度与边类型感知的个性化 PageRank 进行混合检索。
result: "在 LoCoMo-10 基准测试中，GAAMA 达到了 78.9% 的平均奖励，显著超过了 RAG (75.0%) 和 HippoRAG (69.9%) 等现有方法。"
conclusion: 通过将图遍历排名与语义搜索相结合，GAAMA 有效增强了智能体在复杂、跨会话场景下的长期记忆处理能力。
---

## 摘要
在多个会话中与用户交互的 AI 智能体需要持久的长期记忆，以保持连贯、个性化的行为。目前的方法要么依赖于扁平的检索增强生成 (RAG)，这会丢失记忆之间的结构关系；要么使用记忆压缩和向量检索，无法捕捉多会话对话的关联结构。文献中虽然提出了一些基于图的技术，但它们仍面临枢纽节点主导检索以及对演化记忆的层次化推理能力不足的问题。我们提出了 GAAMA，一种图增强关联记忆系统，通过三步流水线构建以概念为中介的层次化知识图谱：(1) 从原始对话中逐字保留片段 (episode)；(2) 基于大语言模型 (LLM) 提取原子事实和主题级概念节点；(3) 合成高阶反思 (reflection)。生成的图包含四种节点类型（片段、事实、反思、概念），由五种结构化边类型连接，其中概念节点提供了补充语义相似性的横向遍历路径。检索过程通过加法评分函数，将基于余弦相似度的 k-最近邻搜索与感知边类型的个性化 PageRank (PPR) 相结合。在 LoCoMo-10 基准测试（涵盖 10 个多会话对话中的 1,540 个问题）上，GAAMA 达到了 78.9% 的平均奖励，优于经过调优的 RAG 基准 (75.0%)、HippoRAG (69.9%)、A-Mem (47.2%) 和 Nemori (52.1%)。消融实验表明，将基于图遍历的排序（个性化 PageRank）与语义搜索相结合，相比于纯图节点语义搜索，性能得到持续提升（整体提升 1.0 个百分点）。

## Abstract
AI agents that interact with users across multiple sessions require persistent long-term memory to maintain coherent, personalized behavior. Current approaches either rely on flat retrieval-augmented generation (RAG), which loses structural relationships between memories, or use memory compression and vector retrieval that cannot capture the associative structure of multi-session conversations. There are few graph based techniques proposed in the literature, however they still suffer from hub dominated retrieval and poor hierarchical reasoning over evolving memory. We propose GAAMA, a graph-augmented associative memory system that constructs a concept-mediated hierarchical knowledge graph through a three-step pipeline: (1)~verbatim episode preservation from raw conversations, (2)~LLM-based extraction of atomic facts and topic-level concept nodes, and (3)~synthesis of higher-order reflections. The resulting graph uses four node types (episode, fact, reflection, concept) connected by five structural edge types, with concept nodes providing cross-cutting traversal paths that complement semantic similarity. Retrieval combines cosine-similarity-based $k$-nearest neighbor search with edge-type-aware Personalized PageRank (PPR) through an additive scoring function. On the LoCoMo-10 benchmark (1,540 questions across 10 multi-session conversations), GAAMA achieves 78.9\% mean reward, outperforming a tuned RAG baseline (75.0\%), HippoRAG (69.9\%), A-Mem (47.2\%), and Nemori (52.1\%). Ablation analysis shows that augmenting graph-traversal-based ranking (Personalized PageRank) with semantic search consistently improves over pure semantic search on graph nodes (+1.0 percentage point overall).

---

## 论文详细总结（自动生成）

以下是对论文《GAAMA: Graph Augmented Associative Memory for Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
AI 智能体在跨会话（multi-session）交互中需要持久的长期记忆（LTM）来保持行为的连贯性和个性化。现有的方法存在以下局限：
*   **扁平 RAG：** 仅依赖向量相似度检索文本块，丢失了实体、事件和事实之间的结构化关联。
*   **实体中心化图结构（如 HippoRAG）：** 在多轮对话中，特定实体（如“用户”）会积累数百条边，形成“超级枢纽（Mega-hubs）”，导致个性化 PageRank（PPR）权重稀释，降低检索精度。
*   **上下文丢失：** 压缩记忆或简单的向量检索难以处理复杂的时序推理和跨会话的模式合成。

**GAAMA 的核心目标**是构建一种以“概念（Concept）”为中介的层次化知识图谱，通过结构化关联和混合检索算法，提升智能体对长期记忆的召回质量和推理能力。

### 2. 方法论：核心思想与关键技术
GAAMA 提出了一个三步构建流水线和一种混合检索机制：

#### A. 三步知识图谱构建
1.  **片段保留（Episode Preservation）：** 逐字存储对话回合，不进行 LLM 压缩，利用 `NEXT` 边连接，保留原始时序参考（如“昨天”）。
2.  **事实与概念提取（Fact & Concept Extraction）：** 使用 LLM 提取原子事实（Facts）和主题级概念（Concepts，如 `pottery_hobby`）。概念节点替代了容易成为枢纽的实体节点，使图结构更稀疏且具区分度。
3.  **反思合成（Reflection Synthesis）：** LLM 跨多个事实合成高阶洞察（如“用户偏好周末户外活动”），用于处理复杂的推理查询。

#### B. 混合检索算法
*   **候选池与种子选择：** 首先通过 KNN 检索 2B 个候选节点，选取前 $k$ 个作为 PPR 的种子节点。
*   **感知边类型的 PPR：** 在局部子图中运行 PPR，根据边类型（如 `DERIVED_FROM`）分配不同的转移权重，并引入**枢纽抑制（Hub Dampening）**机制，限制高出度节点的权重扩散。
*   **加法评分函数：** 最终得分结合了归一化的 PPR 分数和语义相似度分数：
    $$Score(n) = w_{ppr} \cdot PPR(n) + w_{sim} \cdot Sim(n, q)$$
    其中 $w_{ppr}=0.1$，$w_{sim}=1.0$，强调图结构作为语义检索的补充而非替代。

### 3. 实验设计
*   **数据集：** 使用 **LoCoMo-10** 基准测试，包含 10 个多会话对话，共 1,540 个问题。
*   **问题分类：** 多跳推理（Cat1）、时序推理（Cat2）、开放域（Cat3）和单跳事实（Cat4）。
*   **对比方法：**
    *   **A-Mem：** 基于 Zettelkasten 的笔记式记忆。
    *   **Nemori：** 基于认知科学的自组织记忆。
    *   **HippoRAG：** 经典的实体中心化图检索系统。
    *   **RAG Baseline：** 经过调优的扁平向量检索系统。
*   **评估指标：** 使用 LLM-as-judge（GPT-4o-mini）根据参考答案的关键事实覆盖率给出 0.0-1.0 的奖励分数。

### 4. 资源与算力
*   **模型使用：** 提取、生成和评估均使用 `GPT-4o-mini`；嵌入模型使用 `text-embedding-3-small`。
*   **算力说明：** 论文**未明确说明**具体的硬件算力（如 GPU 型号、数量或训练时长）。由于该系统主要依赖于调用 LLM API 进行推理和构建，不涉及大规模模型预训练，其算力消耗主要集中在 API 调用成本和本地图运算上。

### 5. 实验数量与充分性
*   **实验规模：** 针对 1,540 个问题进行了详尽测试，样本量足以支撑统计显著性。
*   **消融实验：** 进行了“纯语义检索”与“PPR 增强检索”的对比，并按对话和问题类别进行了热图分析。
*   **公平性：** 所有基准测试均使用相同的 LLM 生成答案和相同的评估协议，确保了检索质量是唯一的变量。实验设计较为客观，涵盖了从单跳到复杂时序推理的多种场景。

### 6. 主要结论与发现
*   **性能领先：** GAAMA 整体奖励分数为 **78.9%**，显著优于 RAG 基准（75.0%）和 HippoRAG（69.9%）。
*   **时序推理优势：** 在时序问题上，GAAMA 比 RAG 高出 **12.9%**，证明了 `NEXT` 边和原始片段保留的有效性。
*   **图结构的增益：** 消融实验显示，加入 PPR 增强后，整体性能提升了 1.0%，尤其在单跳问题上提升了 1.6%。
*   **权重平衡：** 实验发现轻微的图增强（$w_{ppr}=0.1$）效果最好，过强的图权重会引入噪声。

### 7. 优点
*   **创新的概念中介设计：** 有效解决了实体中心化图结构中的“超级枢纽”问题，使图遍历更具语义聚焦性。
*   **层次化记忆结构：** 通过“片段-事实-反思”的层次，既保留了原始上下文，又提取了高阶知识。
*   **三步构建流水线：** 将廉价的结构化操作与昂贵的 LLM 提取分离，支持记忆的持续演化。

### 8. 不足与局限
*   **概念冗余：** LLM 提取的概念存在近义词或单复数重复（如 `book_recommendation` 与 `book_recommendations`），导致图结构碎片化。
*   **PPR 增益有限：** 相比于复杂的图构建过程，PPR 带来的 1.0% 提升显得相对较小，暗示系统的大部分收益来自于 LLM 对事实和反思的预处理。
*   **开放域表现波动：** 在开放域问题上，图遍历有时会引入无关信息，导致性能出现负向波动。
*   **依赖 LLM 质量：** 记忆的质量高度依赖于提取阶段 LLM 的推理能力，若提取错误，图结构会产生误导。

（完）
