---
title: "GAAMA: Graph Augmented Associative Memory for Agents"
title_zh: GAAMA：面向智能体的图增强关联记忆系统
authors: "Swarna Kamal Paul, Shubhendu Sharma, Nitin Sareen"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27910v1"
tags: ["query:mmkgc-llm"]
score: 9.0
evidence: 智能体的图增强关联记忆
tldr: 针对多会话AI智能体长期记忆缺失结构化关联的问题，本文提出GAAMA系统。该系统通过提取原子事实、主题概念及高阶反思，构建包含四类节点和五类边的分层知识图谱。检索时结合语义相似度与感知边类型的个性化PageRank算法。实验表明，GAAMA在LoCoMo-10基准上显著优于RAG和HippoRAG等基准，有效提升了记忆检索的准确性与层次化推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27910v1/fig-001.webp\", \"caption\": \"Table 7: Heatmap of reward delta (pp): PPR=0.1 − Semantic, per conversation and category.\", \"page\": 10, \"index\": 1, \"width\": 629, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-002.webp\", \"caption\": \"Table 1: GAAMA graph schema: four node types connected by five structural edge types.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-003.webp\", \"caption\": \"Table 6: Per-conversation overall reward (%) for Semantic vs. PPR=0.1. ∆ = PPR=0.1 − Semantic (pp).\", \"page\": 9, \"index\": 3, \"width\": 527, \"height\": 276}]"
motivation: 现有的RAG和向量检索方法难以捕捉多会话对话中的关联结构，且现有图技术在处理演化记忆时存在层次化推理不足的问题。
method: 构建一个包含片段、事实、反思和概念四种节点的分层图谱，并结合语义搜索与个性化PageRank（PPR）进行混合检索。
result: "在LoCoMo-10基准测试中，GAAMA以78.9%的平均奖励得分超越了RAG（75.0%）和HippoRAG（69.9%）等主流方法。"
conclusion: 通过将图遍历算法与语义搜索相结合，GAAMA成功增强了智能体对长期、复杂记忆的关联检索与层次化理解能力。
---

## 摘要
在多会话中与用户交互的 AI 智能体需要持久的长期记忆，以维持连贯且个性化的行为。目前的方法要么依赖于扁平的检索增强生成（RAG），这会丢失记忆间的结构关系；要么使用记忆压缩和向量检索，无法捕捉多会话对话的关联结构。文献中虽有少量基于图的技术被提出，但它们仍面临枢纽节点主导检索以及对演化记忆的层次化推理能力不足的问题。我们提出了 GAAMA，一种图增强关联记忆系统，它通过三步流水线构建以概念为中介的层次化知识图谱：(1) 从原始对话中逐字保留片段（episode）；(2) 基于大语言模型（LLM）提取原子事实和主题级概念节点；(3) 合成高阶反思（reflection）。生成的图包含四种节点类型（片段、事实、反思、概念），由五种结构化边类型连接，其中概念节点提供了与语义相似性互补的横向遍历路径。检索过程通过加法评分函数，将基于余弦相似度的 k-最近邻搜索与感知边类型的个性化 PageRank（PPR）相结合。在 LoCoMo-10 基准测试（涵盖 10 个多会话对话中的 1,540 个问题）上，GAAMA 实现了 78.9% 的平均奖励，优于经过调优的 RAG 基准（75.0%）、HippoRAG（69.9%）、A-Mem（47.2%）和 Nemori（52.1%）。消融实验表明，在图节点上将基于图遍历的排序（个性化 PageRank）与语义搜索相结合，相比纯语义搜索在性能上有一致的提升（整体提升 1.0 个百分点）。

## Abstract
AI agents that interact with users across multiple sessions require persistent long-term memory to maintain coherent, personalized behavior. Current approaches either rely on flat retrieval-augmented generation (RAG), which loses structural relationships between memories, or use memory compression and vector retrieval that cannot capture the associative structure of multi-session conversations. There are few graph based techniques proposed in the literature, however they still suffer from hub dominated retrieval and poor hierarchical reasoning over evolving memory. We propose GAAMA, a graph-augmented associative memory system that constructs a concept-mediated hierarchical knowledge graph through a three-step pipeline: (1)~verbatim episode preservation from raw conversations, (2)~LLM-based extraction of atomic facts and topic-level concept nodes, and (3)~synthesis of higher-order reflections. The resulting graph uses four node types (episode, fact, reflection, concept) connected by five structural edge types, with concept nodes providing cross-cutting traversal paths that complement semantic similarity. Retrieval combines cosine-similarity-based $k$-nearest neighbor search with edge-type-aware Personalized PageRank (PPR) through an additive scoring function. On the LoCoMo-10 benchmark (1,540 questions across 10 multi-session conversations), GAAMA achieves 78.9\% mean reward, outperforming a tuned RAG baseline (75.0\%), HippoRAG (69.9\%), A-Mem (47.2\%), and Nemori (52.1\%). Ablation analysis shows that augmenting graph-traversal-based ranking (Personalized PageRank) with semantic search consistently improves over pure semantic search on graph nodes (+1.0 percentage point overall).

---

## 论文详细总结（自动生成）

以下是对论文《GAAMA: Graph Augmented Associative Memory for Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：AI 智能体在跨多会话交互时，如何构建并检索高效的长期记忆（LTM），以保持行为的连贯性和个性化。
*   **研究背景**：
    *   **扁平 RAG 的局限**：传统的检索增强生成（RAG）依赖向量相似度，丢失了记忆间的结构化关联（如事件顺序、实体关系）。
    *   **现有图方法的缺陷**：如 HippoRAG 等基于实体的图方法在多会话场景下会产生“超级枢纽（Mega-hubs）”问题（如“用户”节点连接数过多），导致检索精度稀释；同时，现有方法在处理演化记忆的层次化推理上表现较差。

### 2. 方法论：GAAMA 系统架构
GAAMA 的核心思想是构建一个**以概念为中介的层次化知识图谱**，并通过混合检索算法提取信息。

*   **三步图构建流水线**：
    1.  **片段保留（Episode Preservation）**：无需 LLM，逐字存储对话回合，通过 `NEXT` 边连接，保留原始语境和时间戳，便于时间推理。
    2.  **事实与概念提取（Fact & Concept Extraction）**：利用 LLM 提取原子事实（Atomic Facts）和主题级概念（Concepts，如 `pottery_hobby`）。概念节点替代了传统的实体节点，避免了枢纽节点过载。
    3.  **反思合成（Reflection Synthesis）**：LLM 综合多个事实生成高阶洞察（如“用户偏好周末户外活动”），捕捉跨会话的模式。
*   **图模式（Schema）**：包含 4 种节点（片段、事实、反思、概念）和 5 种边（时间先后、来源推导、主题关联等）。
*   **混合检索算法**：
    *   **KNN 候选提取**：基于余弦相似度选取前 $k$ 个种子节点。
    *   **感知边类型的 PPR**：在局部子图上运行个性化 PageRank，根据边类型分配不同权重，并引入**枢纽抑制（Hub Dampening）**机制。
    *   **加法评分函数**：最终得分 = $1.0 \times \text{语义相似度} + 0.1 \times \text{PPR得分}$。这种设计让图结构起到“微调”和“关联扩展”的作用，而非完全取代语义搜索。

### 3. 实验设计
*   **数据集/基准**：使用 **LoCoMo-10** 基准，包含 10 个长篇多会话对话，共 1,540 个问题。
*   **问题分类**：多跳推理（Cat1）、时间推理（Cat2）、开放域（Cat3）和单跳事实（Cat4）。
*   **对比方法**：
    *   **A-Mem**：基于 Zettelkasten 笔记系统的智能体记忆。
    *   **Nemori**：受认知科学启发的自组织记忆。
    *   **HippoRAG**：基于实体的图检索系统。
    *   **Tuned RAG Baseline**：经过优化的扁平向量检索系统（使用相同的嵌入模型和上下文预算）。
*   **评估指标**：LLM-as-judge 评分（基于关键事实覆盖率的 0.0-1.0 奖励分）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或训练时长）。
*   **模型使用**：实验主要依赖 API 调用。使用 `GPT-4o-mini` 进行知识提取、答案生成和结果评估；使用 `text-embedding-3-small` 生成 1536 维的节点嵌入。

### 5. 实验数量与充分性
*   **实验规模**：在 1,540 个测试样本上进行了完整测试，样本量充足。
*   **消融实验**：对比了“纯语义检索”与“PPR 增强检索”，验证了图结构带来的增量收益。
*   **客观性**：所有基准测试均使用相同的生成模型和评估提示词，确保了检索质量是唯一的变量，实验设计较为公平。

### 6. 主要结论与发现
*   **性能领先**：GAAMA 实现了 **78.9%** 的平均奖励分，显著优于 RAG 基准（75.0%）和 HippoRAG（69.9%）。
*   **擅长复杂推理**：在**时间推理（Cat2）**上提升最显著（比 RAG 高 12.9%），在**多跳推理（Cat1）**上也表现优异。
*   **图结构的价值**：消融实验显示，加入 PPR 增强后，整体性能提升了 1.0 个百分点，尤其在单跳和时间类问题上提供了更精准的上下文。
*   **层次化构建是关键**：即使不使用 PPR，仅靠 GAAMA 构建的“事实+反思+片段”结构进行语义检索，也比原始 RAG 高出 3.0%。

### 7. 优点与亮点
*   **层次化记忆模型**：通过“片段-事实-反思”的递进结构，成功模拟了人类记忆从感知到抽象的过程。
*   **概念中介设计**：巧妙地解决了知识图谱在对话场景下的“超级枢纽”问题，使图结构更稀疏、检索更高效。
*   **时间感知**：通过 `NEXT` 边和原始片段保留，解决了 LLM 在处理“上周”、“昨天”等相对时间参考时的痛点。

### 8. 不足与局限
*   **概念冗余**：LLM 提取的概念存在近义词或单复数重复（如 `book_recommendation` 与 `book_recommendations`），导致图结构碎片化。
*   **PPR 增益有限**：虽然图增强有提升，但 1.0% 的绝对提升相对较小，且在某些开放域问题中可能引入噪声。
*   **依赖 LLM 质量**：图的构建高度依赖 LLM 的提取能力，若 LLM 在第一步提取了错误事实，后续检索将受限。
*   **静态权重**：PPR 的边权重是人工设定的，而非通过学习获得，可能无法适配所有类型的对话。

（完）
